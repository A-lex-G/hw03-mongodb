const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const validateFavoriteField = require("./validateFavorite");

module.exports = {
  validateBody,
  isValidId,
  validateFavoriteField,
};
